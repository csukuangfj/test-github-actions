name: test-docker

on:
  push:
    branches:
      - test-docker
jobs:
  test_docker:
    name: Test docker
    container: nvidia/cuda:10.2-cudnn7-devel-ubuntu18.04
    steps:
      - name: Run the build process with Docker
        shell: bash
        run: |
          ls -lh
          which nvcc
